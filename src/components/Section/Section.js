import {Section, Title} from './SectionStyles'

export default function SectionFunc({ children, title  }) {
    
    return <Section><Title></Title>{children}</Section>
/* <Button name='good' onClick={(e) => this.handleIncrement(e)}>Good</Button> */
}